-r requirements.txt

# Runtime dependencies for the vendored `dex_retargeting` package used by
# `dexslide/retargeting/` and `scripts/publish_dex_orca_targets.py`.
# Keep NumPy below 2.x for now because common `nlopt` wheels used by this
# retarget stack are still built against the NumPy 1.x ABI.
numpy>=1.24,<2
pytransform3d>=3.5.0
pin>=2.7.0
nlopt>=2.6.1,<2.8.0
trimesh>=4.4.0
anytree>=2.12.0
lxml>=5.2.2
torch
